   [오늘의 주제는 클래스 타입]
1. 기본형 8가지를 제외하고 다 클래스타입이다.
2. 변수 타입으로 기본형 8가지 빼고 클래스이름이 온다.
3. 메소드 호출시 파라미터의 타입을 잘 맞춰주면 된다.
4. 변수의 값을 넣을 때, zoo.bird.name = "부산갈매기";
   => String name = zoo.bird.name;
5. 파라미터로 객체를 넣어도 그 메소드가 끝나면 없어진다.
6. A a = new A();
   A a2 = a;
   a2.name = "미영";
   System.out.println(a.name);
   
7. 클래스타입의 파라미터를 가진 메소드를 호출시 방법이 2가지.
   1)바로 new
   2)변수 선언해서 그 변수를 이용하는 경우
   
8. 생성자에서 객체변수를 생성하고 그 안에서 사용 할 수 있다.
   new 생성자 => 객체 => 객체변수 => 주소.주소.name